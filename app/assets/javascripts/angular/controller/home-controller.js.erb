app.controller('HomeCtrl', ['$scope', '$http', function($scope, $http){

  //No longer using a set image as the pin backgroun image
  // $scope.image = {
  //   'background-image': "url(" + "<%= asset_path('Minion-Coffee.jpg') %>" + ")"
  // };

  $http.get('pins.json').success(function(data){
    console.log('sucess on getting pins from HomeCtrl');
    // console.log(data);
    $scope.pins = data.pins;
    var numberOfPins = data.pins.length;
    // console.log(numberOfPins);
    // console.log(data.pins[numberOfPins-1]);
    // console.log('from controller I see', angular.element('.box').length,'boxes');
  });

  $http.get('/categories.json').success(function(data){
  console.log('sucess on get categories');
  // console.log(data);
  $scope.categories = data.categories;
  })

  
}])

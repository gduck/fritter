app.directive('indexBody', ['$http', function($http){
  // Runs during compile
  return {
    // name: '',
    // priority: 1,
    // terminal: true,
    // scope: {}, // {} = isolate, true = child, false/undefined = no change
    // controller: function($scope, $element, $attrs, $transclude) {},
    // require: 'ngModel', // Array = multiple requires, ? = optional, ^ = check parent elements
    restrict: 'E', // E = Element, A = Attribute, C = Class, M = Comment
    // template: '',
    templateUrl: "<%= asset_path('pin-index.html') %>",
    // controller: 'HomeCtrl',
    // replace: true,
    // transclude: true,
    // compile: function(tElement, tAttrs, function transclude(function(scope, cloneLinkingFn){ return function linking(scope, elm, attrs){}})),
    // link: function(scope, iElm, iAttrs, controller) {
    //   if (scope.$last === true){
        
    //     $timeout(function () {
    //       // console.log('from directive I see', angular.element('.pin-outer').length,'.pin-outer');
    //       // console.log($('#masonry-container'));
    //       console.log("it works");
    //       $('#masonry-container').masonry({
    //         // itemSelector: '.pin-outer',
    //         // columnWidth: 230,
    //         // gutterWith: 50,
    //         // isAnimated: !Modernizr.csstransitions,
    //         // isRTL: true,
    //         // isFitWidth: true
    //       });
    //     });
    //   }
    // }
  };
}]);

app.directive('onFinishRender', ['$timeout', function ($timeout){
  return {
    restrict: 'A',
    link: function(scope, element, attr){
      if (scope.$last === true){

        console.log("yes on finish render @indexBody");
        
        $timeout(function () {
          console.log('from directive I see', angular.element('.pin-outer').length,'.pin-outer');
          $('#masonry-container').masonry({
            // itemSelector: '.pin-outer',
            // columnWidth: 230,
            // gutterWith: 50,
            // isAnimated: !Modernizr.csstransitions,
            // isRTL: true,
            // isFitWidth: true
          });
        });
      }
    }
  }
}]);